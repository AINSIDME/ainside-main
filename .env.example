# Copy this file to `.env` and fill in your PayPal client ID.
# Vite will expose these to the client at build time.

VITE_PAYPAL_CLIENT_ID=REPLACE_WITH_YOUR_PAYPAL_CLIENT_ID
# sandbox or live
VITE_PAYPAL_ENV=sandbox
